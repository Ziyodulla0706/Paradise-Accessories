# Paradise Accessories - Полная Документация Проекта

## 📋 Оглавление

1. [Описание проекта](#описание-проекта)
2. [Архитектура проекта](#архитектура-проекта)
3. [Технологический стек](#технологический-стек)
4. [Структура проекта](#структура-проекта)
5. [Установка и настройка](#установка-и-настройка)
6. [Конфигурация](#конфигурация)
7. [API Документация](#api-документация)
8. [Функциональность](#функциональность)
9. [Модели данных](#модели-данных)
10. [Интеграции](#интеграции)
11. [Развертывание](#развертывание)
12. [Безопасность](#безопасность)
13. [Поддержка и обслуживание](#поддержка-и-обслуживание)

---

## 🎯 Описание проекта

**Paradise Accessories** — это многоязычный корпоративный B2B веб-сайт для компании, производящей премиальные бирки, этикетки и аксессуары для одежды. Проект предоставляет полный цикл управления заявками, контентом и аналитикой для B2B бизнеса.

### Основные возможности:
- ✅ **Многоязычная поддержка** (Русский, Английский, Узбекский)
- ✅ **Управление заявками (Leads)** с полным жизненным циклом
- ✅ **Контактные формы** с загрузкой файлов
- ✅ **Портфолио и каталог продукции** с мультиязычным контентом
- ✅ **Аналитика и отслеживание** пользовательских действий
- ✅ **Email уведомления** (Gmail SMTP)
- ✅ **Telegram уведомления** для мгновенных оповещений
- ✅ **Админ-панель Django** для управления контентом
- ✅ **REST API** для интеграций
- ✅ **Экспорт данных** в CSV
- ✅ **Защита от спама** (Rate limiting)
- ✅ **Современный адаптивный дизайн**

---

## 🏗️ Архитектура проекта

Проект построен по классической архитектуре **клиент-сервер**:

```
┌─────────────────────────────────────────────────┐
│           Frontend (HTML/CSS/JS)                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │   RU/    │  │   EN/    │  │   UZ/    │     │
│  │  Pages   │  │  Pages   │  │  Pages   │     │
│  └──────────┘  └──────────┘  └──────────┘     │
│         │              │              │         │
│         └──────────────┼──────────────┘         │
│                        │                        │
│            ┌───────────▼───────────┐            │
│            │   API Client (JS)     │            │
│            └───────────┬───────────┘            │
└────────────────────────┼────────────────────────┘
                         │ HTTP/REST
┌────────────────────────┼────────────────────────┐
│           Backend (Django REST API)             │
│  ┌──────────────────────────────────────────┐  │
│  │         Django REST Framework            │  │
│  └──────────────────────────────────────────┘  │
│         │         │         │         │        │
│  ┌──────▼───┐ ┌──▼───┐ ┌───▼───┐ ┌───▼───┐   │
│  │  Leads   │ │Content│ │Analyt.│ │ Core  │   │
│  │   App    │ │  App  │ │  App  │ │ Utils │   │
│  └──────┬───┘ └───┬───┘ └───┬───┘ └───┬───┘   │
│         │         │         │         │        │
│         └─────────┼─────────┼─────────┘        │
│                   │         │                   │
│         ┌─────────▼─────────▼─────────┐        │
│         │     Database (SQLite/Postgres)       │
│         └─────────────────────────────┘        │
│                   │                            │
│         ┌─────────▼─────────┐                  │
│         │  External Services │                  │
│         │  - Gmail SMTP      │                  │
│         │  - Telegram Bot    │                  │
│         └────────────────────┘                  │
└─────────────────────────────────────────────────┘
```

---

## 💻 Технологический стек

### Backend
- **Python 3.10+**
- **Django 5.0** — основной веб-фреймворк
- **Django REST Framework 3.14.0** — API
- **django-cors-headers 4.3.1** — CORS для фронтенда
- **django-environ 0.11.2** — управление переменными окружения
- **django-filter 23.5** — фильтрация данных
- **django-ratelimit 4.1.0** — защита от спама
- **Pillow 10.2.0** — обработка изображений
- **psycopg2-binary 2.9.9** — драйвер PostgreSQL
- **requests 2.31.0** — HTTP-клиент для Telegram API

### Frontend
- **HTML5** — разметка
- **CSS3** — стилизация (современные CSS переменные, адаптивный дизайн)
- **Vanilla JavaScript** — интерактивность
- **Google Fonts (Inter)** — типографика

### База данных
- **SQLite** — для разработки
- **PostgreSQL** — для production

### Инфраструктура
- **Nginx/Apache** — веб-сервер (production)
- **Gunicorn/uWSGI** — WSGI-сервер (production)

---

## 📁 Структура проекта

```
Paradise-Accessories/
│
├── backend/                          # Django Backend
│   ├── manage.py                     # Django management script
│   ├── db.sqlite3                    # SQLite база данных (dev)
│   │
│   ├── paradise_backend/             # Главный проект Django
│   │   ├── __init__.py
│   │   ├── settings.py               # Настройки проекта
│   │   ├── urls.py                   # Главная URL конфигурация
│   │   ├── wsgi.py                   # WSGI конфигурация
│   │   └── asgi.py                   # ASGI конфигурация
│   │
│   ├── leads/                        # Приложение для управления заявками
│   │   ├── models.py                 # Модель Lead
│   │   ├── views.py                  # API endpoints для заявок
│   │   ├── serializers.py            # DRF сериализаторы
│   │   ├── urls.py                   # URL маршруты для leads
│   │   └── admin.py                  # Django admin настройки
│   │
│   ├── content/                      # Приложение для контента
│   │   ├── models.py                 # PortfolioItem, Product, ProductImage
│   │   ├── views.py                  # API для портфолио и продуктов
│   │   ├── serializers.py            # Сериализаторы контента
│   │   ├── urls.py                   # URL маршруты
│   │   └── admin.py                  # Админ панель для контента
│   │
│   ├── analytics/                    # Приложение аналитики
│   │   ├── models.py                 # AnalyticsEvent модель
│   │   ├── views.py                  # API для аналитики
│   │   ├── serializers.py            # Сериализаторы
│   │   └── urls.py                   # URL маршруты
│   │
│   ├── core/                         # Основные утилиты
│   │   ├── utils/
│   │   │   ├── email.py              # Email уведомления
│   │   │   ├── telegram.py           # Telegram бот интеграция
│   │   │   └── helpers.py            # Вспомогательные функции
│   │   └── views.py                  # Общие views
│   │
│   └── media/                        # Загруженные файлы
│       └── portfolio/                # Изображения портфолио
│
├── assets/                           # Статические файлы фронтенда
│   ├── css/
│   │   └── style.css                 # Главный файл стилей
│   ├── js/
│   │   ├── main.js                   # Основная логика
│   │   ├── config.js                 # Конфигурация API
│   │   ├── i18n.js                   # Интернационализация
│   │   ├── catalog.js                # Логика каталога
│   │   ├── portfolio.js              # Логика портфолио
│   │   └── background-animation.js   # Анимации фона
│   └── images/                       # Изображения
│
├── ru/                               # Русская версия сайта
│   ├── index.html                    # Главная страница
│   ├── about.html                    # О компании
│   ├── catalog.html                  # Каталог
│   ├── portfolio.html                # Портфолио
│   ├── pricing.html                  # Цены
│   ├── contacts.html                 # Контакты
│   └── privacy.html                  # Политика конфиденциальности
│
├── en/                               # Английская версия
│   └── [те же файлы что и в ru/]
│
├── uz/                               # Узбекская версия
│   └── [те же файлы что и в ru/]
│
├── index.html                        # Редирект на основную версию
├── requirements.txt                  # Python зависимости
├── robots.txt                        # Robots.txt для SEO
├── sitemap.xml                       # XML карта сайта
├── README.md                         # Эта документация
├── CHANGELOG.md                      # История изменений
└── DEPLOYMENT.md                     # Инструкции по развертыванию
```

---

## 🚀 Установка и настройка

### Предварительные требования

- Python 3.10 или выше
- pip (менеджер пакетов Python)
- Git (для клонирования репозитория)

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd Paradise-Accessories
```

### Шаг 2: Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

**Важно:** Если возникают проблемы с установкой `Pillow 10.2.0` на Python 3.14+, попробуйте установить более новую версию:
```bash
pip install Pillow>=10.3.0
```

### Шаг 4: Настройка переменных окружения

Создайте файл `.env` в корне проекта (`Paradise-Accessories/.env`):

```env
# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-here-generate-with-django-inspect-secret-key
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite для разработки, PostgreSQL для production)
DATABASE_URL=sqlite:///db.sqlite3
# Для PostgreSQL: DATABASE_URL=postgresql://user:password@localhost:5432/paradise_db

# Email Configuration (Gmail SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=your-email@gmail.com
ADMIN_EMAIL=admin@yourdomain.com

# Telegram Bot (опционально)
TELEGRAM_BOT_TOKEN=your-bot-token-here
TELEGRAM_CHAT_ID=your-chat-id-here

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# CSRF Settings
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

### Шаг 5: Миграции базы данных

```bash
cd backend
python manage.py migrate
```

### Шаг 6: Создание суперпользователя

```bash
python manage.py createsuperuser
```

Введите логин, email и пароль для доступа к админ-панели.

### Шаг 7: Загрузка статических файлов (для production)

```bash
python manage.py collectstatic
```

### Шаг 8: Запуск сервера разработки

```bash
python manage.py runserver
```

Сервер будет доступен по адресу: `http://localhost:8000`

- **API:** http://localhost:8000/api/
- **Админ-панель:** http://localhost:8000/admin/
- **Фронтенд:** http://localhost:8000/ru/ (и другие языковые версии)

---

## ⚙️ Конфигурация

### Настройка Email (Gmail)

1. Перейдите в настройки Google аккаунта
2. Включите двухфакторную аутентификацию
3. Создайте пароль приложения:
   - Перейдите в [Google Account Security](https://myaccount.google.com/security)
   - Выберите "2-Step Verification" → "App passwords"
   - Создайте пароль для "Mail"
   - Используйте этот пароль в `.env` как `EMAIL_HOST_PASSWORD`

### Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/botfather)
   ```
   /newbot
   ```
   Следуйте инструкциям и получите токен бота.

2. Получите Chat ID:
   - Напишите боту любое сообщение
   - Откройте: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - Найдите `chat.id` в ответе

3. Добавьте в `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=your-bot-token
   TELEGRAM_CHAT_ID=your-chat-id
   ```

### Настройка API endpoints (Frontend)

Отредактируйте `assets/js/config.js`:

```javascript
window.API_CONFIG = {
    // Для разработки
    baseURL: 'http://localhost:8000',
    
    // Для production
    // baseURL: 'https://api.yourdomain.com',
    // или оставьте пустым для same-origin
};
```

---

## 📡 API Документация

### Базовый URL
```
http://localhost:8000/api/
```

### Публичные эндпоинты

#### 1. Отправка контактной формы
```http
POST /api/contact/submit/
Content-Type: multipart/form-data

{
    "name": "Иван Иванов",
    "company": "ООО Компания",
    "phone": "+998901234567",
    "email": "ivan@example.com",  // опционально
    "product_type": "woven",       // woven|printed|hang_tags|stickers|other
    "quantity": 1000,              // опционально
    "message": "Интересуют вшивные этикетки",
    "file": <file>,                // опционально (PDF, DOC, изображения)
    "language": "ru",              // ru|en|uz
    "source": "website"            // опционально
}
```

**Ответ (успех):**
```json
{
    "success": true,
    "message": "Спасибо! Ваша заявка принята. Мы свяжемся с вами в ближайшее время.",
    "lead_id": 123
}
```

**Лимит:** 5 запросов в час с одного IP адреса.

#### 2. Отслеживание аналитики
```http
POST /api/analytics/track/
Content-Type: application/json

{
    "event_type": "page_view",     // page_view|form_submit|form_start|file_upload|button_click|link_click
    "page": "/ru/index.html",
    "language": "ru",
    "referrer": "https://google.com",
    "session_id": "abc123xyz",
    "metadata": {                   // опционально
        "form_type": "contact"
    }
}
```

#### 3. Портфолио (публичное)
```http
GET /api/content/portfolio/?lang=ru&category=woven
```

**Параметры запроса:**
- `lang` - язык (ru|en|uz), по умолчанию: ru
- `category` - фильтр по категории (woven|printed|hang_tags|stickers|packaging)

**Ответ:**
```json
[
    {
        "id": 1,
        "image": "/media/portfolio/2026/01/image.png",
        "title": "Вшивные этикетки для бренда X",
        "description": "Описание...",
        "category": "woven"
    }
]
```

#### 4. Продукты (публичное)
```http
GET /api/content/products/?lang=ru&category=woven_labels&is_featured=true
GET /api/content/products/{slug}/?lang=ru
```

**Параметры:**
- `lang` - язык
- `category` - категория продукта
- `is_featured` - показать только избранные (true/false)
- `search` - поиск по названию и описанию

### Защищенные эндпоинты (требуется аутентификация)

Для доступа к защищенным эндпоинтам необходимо авторизоваться через Django admin или использовать токены.

#### 5. Управление заявками

**Список заявок:**
```http
GET /api/leads/
Authorization: Session <session-id>
```

**Параметры фильтрации:**
- `status` - статус (new|contacted|qualified|closed|rejected)
- `product_type` - тип продукта
- `language` - язык
- `search` - поиск по имени, компании, телефону, email, сообщению
- `ordering` - сортировка (-created_at, name, company, status)

**Детали заявки:**
```http
GET /api/leads/{id}/
```

**Обновление заявки:**
```http
PUT /api/leads/{id}/
Content-Type: application/json

{
    "status": "contacted",
    "name": "Иван Иванов",
    ...
}
```

**Удаление заявки:**
```http
DELETE /api/leads/{id}/
```

**Статистика заявок:**
```http
GET /api/leads/stats/
```

**Экспорт в CSV:**
```http
GET /api/leads/export/csv/?status=new&start_date=2026-01-01
```

#### 6. Аналитика (защищенное)

**Дашборд статистики:**
```http
GET /api/analytics/stats/
```

**Детальный отчет:**
```http
GET /api/analytics/report/?start_date=2026-01-01&end_date=2026-01-31&event_type=page_view&language=ru
```

---

## 🎨 Функциональность

### Frontend

#### 1. Многоязычность
- **Языки:** Русский, Английский, Узбекский
- **Переключение:** Выпадающее меню в header
- **Сохранение:** Выбранный язык сохраняется в localStorage
- **URL структура:** `/ru/`, `/en/`, `/uz/`

#### 2. Страницы сайта

**Главная страница (`index.html`):**
- Hero секция с CTA
- Преимущества компании
- Каталог продукции (preview)
- Форма обратной связи
- Анимированный фон (floating labels)

**О компании (`about.html`):**
- История компании
- Наша команда
- Преимущества работы с нами

**Каталог (`catalog.html`):**
- Список всех продуктов
- Фильтрация по категориям
- Детальные страницы продуктов
- Галерея изображений

**Портфолио (`portfolio.html`):**
- Примеры работ
- Фильтрация по категориям
- Модальные окна с деталями

**Контакты (`contacts.html`):**
- Контактная форма с валидацией
- Загрузка файлов (до 10MB)
- Карта (если настроена)
- Информация о компании

**Цены (`pricing.html`):**
- Тарифы и прайс-листы
- Калькулятор (если реализован)

#### 3. Контактная форма

**Функции:**
- Валидация полей на клиенте
- Поддержка загрузки файлов (PDF, DOC, изображения)
- Отправка данных через API
- Уведомления об успехе/ошибке
- Аналитика событий (form_start, form_submit, file_upload)

**Типы продуктов:**
- Вшивные этикетки (Woven Labels)
- Печатные этикетки (Printed Labels)
- Навесные бирки (Hang Tags)
- Стикеры (Stickers)
- Другое (Other)

#### 4. Анимации и эффекты

- **Фоновые анимации:** Плавающие этикетки и бирки
- **Адаптивность:** Количество элементов зависит от размера экрана
  - Мобильные: 8 элементов
  - Планшеты: 12 элементов
  - Десктоп: 20 элементов
- **Поддержка prefers-reduced-motion** для доступности

### Backend

#### 1. Управление заявками (Leads)

**Жизненный цикл заявки:**
1. **new** - Новая заявка
2. **contacted** - Связались с клиентом
3. **qualified** - Квалифицированная заявка
4. **closed** - Закрыта (успешная продажа)
5. **rejected** - Отклонена

**Автоматические действия при создании заявки:**
- Отправка email уведомления администратору
- Отправка автоматического ответа клиенту (если указан email)
- Отправка уведомления в Telegram (если настроено)
- Запись IP адреса и User Agent
- Сохранение метаданных (язык, источник)

#### 2. Контент менеджмент

**Портфолио:**
- Мультиязычные названия и описания
- Категоризация
- Сортировка (поле `order`)
- Публикация/скрытие

**Продукты:**
- Мультиязычные названия и описания
- Галерея изображений
- Категории
- Избранные продукты (featured)
- JSON характеристики
- SEO-friendly URLs (slug)

#### 3. Аналитика

**Отслеживаемые события:**
- `page_view` - Просмотр страницы
- `form_submit` - Отправка формы
- `form_start` - Начало заполнения формы
- `file_upload` - Загрузка файла
- `button_click` - Клик по кнопке
- `link_click` - Клик по ссылке

**Метрики:**
- Общее количество просмотров
- Уникальные сессии
- Отправки форм
- Статистика по дням
- Топ страницы
- Разбивка по языкам и типам событий

#### 4. Уведомления

**Email:**
- Уведомление администратору о новой заявке
- Автоматический ответ клиенту (на языке заявки)
- HTML и plain text форматы

**Telegram:**
- Мгновенное уведомление о новой заявке
- Форматированное сообщение с деталями
- Поддержка Markdown

---

## 🗄️ Модели данных

### Lead (Заявка)

```python
- id: AutoField
- name: CharField (200) - Имя контактного лица
- company: CharField (200) - Компания
- phone: CharField (50) - Телефон
- email: EmailField - Email (опционально)
- product_type: CharField - Тип продукта
- quantity: IntegerField - Количество (опционально)
- message: TextField - Сообщение
- file: FileField - Загруженный файл (опционально)
- status: CharField - Статус заявки (default: 'new')
- source: CharField - Источник заявки
- language: CharField - Язык (default: 'ru')
- ip_address: GenericIPAddressField - IP адрес
- user_agent: TextField - User Agent
- created_at: DateTimeField - Дата создания
- updated_at: DateTimeField - Дата обновления
```

**Индексы:**
- `created_at` (descending)
- `status`
- `product_type`

### PortfolioItem (Элемент портфолио)

```python
- id: AutoField
- image: ImageField - Изображение
- title_ru: CharField (200) - Название (RU)
- title_en: CharField (200) - Название (EN)
- title_uz: CharField (200) - Название (UZ)
- description_ru: TextField - Описание (RU)
- description_en: TextField - Описание (EN)
- description_uz: TextField - Описание (UZ)
- category: CharField - Категория
- order: IntegerField - Порядок сортировки (default: 0)
- is_published: BooleanField - Опубликовано (default: True)
- created_at: DateTimeField
- updated_at: DateTimeField
```

### Product (Продукт)

```python
- id: AutoField
- main_image: ImageField - Основное изображение
- slug: SlugField - URL slug (unique)
- name_ru/en/uz: CharField (200) - Названия
- short_description_ru/en/uz: CharField (300) - Краткие описания
- description_ru/en/uz: TextField - Полные описания
- category: CharField - Категория
- features: JSONField - Характеристики (мультиязычные)
- order: IntegerField - Порядок
- is_published: BooleanField - Опубликовано
- is_featured: BooleanField - Избранное
- created_at: DateTimeField
- updated_at: DateTimeField
```

**Связанные модели:**
- `ProductImage` - Галерея изображений (ForeignKey)

### AnalyticsEvent (Событие аналитики)

```python
- id: AutoField
- event_type: CharField - Тип события
- page: CharField (500) - Страница
- language: CharField (10) - Язык
- referrer: CharField (500) - Реферер
- ip_address: GenericIPAddressField - IP адрес
- user_agent: TextField - User Agent
- session_id: CharField (100) - ID сессии
- metadata: JSONField - Дополнительные данные
- timestamp: DateTimeField - Время события
```

**Индексы:**
- `timestamp` (descending)
- `event_type` + `timestamp`
- `session_id` + `timestamp`

---

## 🔌 Интеграции

### 1. Gmail SMTP

**Настройка:**
- Используется для отправки уведомлений администратору
- Автоматические ответы клиентам
- Требуется пароль приложения Google

**Использование:**
```python
from core.utils.email import send_lead_notification, send_auto_reply

# Уведомление администратору
send_lead_notification(lead)

# Автоответ клиенту
send_auto_reply(lead)
```

### 2. Telegram Bot API

**Настройка:**
- Мгновенные уведомления о новых заявках
- Форматированные сообщения
- Markdown поддержка

**Использование:**
```python
from core.utils.telegram import send_telegram_notification

send_telegram_notification(lead)
```

### 3. Django Admin

**Доступ:**
- URL: `/admin/`
- Требуется суперпользователь

**Управление:**
- Заявки (Leads) - просмотр, редактирование, изменение статуса
- Портфолио - добавление, редактирование, удаление
- Продукты - полное управление контентом
- Аналитика - просмотр событий

---

## 🚢 Развертывание

### Production Checklist

1. **Настройка переменных окружения:**
   - `DEBUG=False`
   - Генерация нового `SECRET_KEY`
   - Настройка `ALLOWED_HOSTS`
   - Настройка PostgreSQL

2. **База данных:**
   ```bash
   python manage.py migrate
   python manage.py createsuperuser
   ```

3. **Статические файлы:**
   ```bash
   python manage.py collectstatic --noinput
   ```

4. **Веб-сервер (Nginx):**
   - Настроить проксирование для `/api/` и `/admin/`
   - Сервирование статических файлов
   - SSL сертификат

5. **WSGI сервер:**
   ```bash
   gunicorn paradise_backend.wsgi:application --bind 127.0.0.1:8000
   ```

Подробные инструкции см. в файле `DEPLOYMENT.md`.

---

## 🔒 Безопасность

### Реализованные меры:

1. **Rate Limiting**
   - Контактная форма: 5 запросов/час с одного IP
   - Защита от спама и DDoS

2. **CORS**
   - Настройка разрешенных источников
   - Только для production доменов

3. **CSRF Protection**
   - Django CSRF middleware
   - Настройка trusted origins

4. **Environment Variables**
   - Секретные ключи в `.env` (не в репозитории)
   - `.env` добавлен в `.gitignore`

5. **File Upload**
   - Ограничение размера (10MB)
   - Валидация расширений файлов
   - Безопасное хранение

6. **SQL Injection Protection**
   - Django ORM (параметризованные запросы)

7. **XSS Protection**
   - Автоматическое экранирование в шаблонах
   - Content Security Policy (рекомендуется настроить)

### Рекомендации для production:

- Включить HTTPS
- Настроить SSL сертификат
- Регулярные обновления зависимостей
- Мониторинг ошибок (Sentry)
- Резервное копирование БД
- Логирование и мониторинг

---

## 🛠️ Поддержка и обслуживание

### Обновление зависимостей

```bash
pip list --outdated
pip install --upgrade <package>
```

### Резервное копирование

**База данных:**
```bash
# SQLite
cp backend/db.sqlite3 backup/db_$(date +%Y%m%d).sqlite3

# PostgreSQL
pg_dump -U user paradise_db > backup/db_$(date +%Y%m%d).sql
```

**Медиа файлы:**
```bash
tar -czf backup/media_$(date +%Y%m%d).tar.gz backend/media/
```

### Логи

Django логи находятся в системных логах или в файлах (если настроено).

### Мониторинг

Рекомендуется настроить:
- Мониторинг uptime
- Отслеживание ошибок (Sentry)
- Мониторинг производительности
- Алерты при проблемах

---

## 📞 Контакты и поддержка

Для вопросов и поддержки обращайтесь к команде разработки.

---

## 📄 Лицензия

[Укажите лицензию проекта]

---

## 🔄 История изменений

См. файл `CHANGELOG.md` для детальной истории изменений проекта.

---

**Последнее обновление:** Январь 2026

**Версия:** 1.0.0
