# Paradise Accessories - Frontend

Современный Next.js фронтенд для Paradise Accessories с поддержкой мультиязычности, темной/светлой темы и плавными анимациями.

## 🚀 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **next-intl** - Интернационализация (ru/en/uz)
- **Framer Motion** - Анимации
- **Dark/Light Theme** - Система тем с сохранением в localStorage

## 📦 Установка

```bash
cd frontend
npm install
# или
yarn install
# или
pnpm install
```

## 🏃 Запуск

### Development

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

### Production Build

```bash
npm run build
npm start
```

## 🌍 Языки

Проект поддерживает 3 языка:
- 🇷🇺 Русский (ru) - по умолчанию
- 🇬🇧 English (en)
- 🇺🇿 O'zbek (uz)

Язык определяется из URL: `/ru/`, `/en/`, `/uz/`

## 🎨 Темы

- **Light** - Светлая тема (по умолчанию)
- **Dark** - Темная тема

Тема сохраняется в localStorage и автоматически применяется при загрузке страницы.

## 📁 Структура проекта

```
frontend/
├── app/
│   ├── [locale]/          # Локализованные страницы
│   │   ├── layout.tsx     # Layout для локалей
│   │   └── page.tsx       # Главная страница
│   ├── globals.css        # Глобальные стили
│   └── layout.tsx         # Root layout
├── components/
│   ├── sections/          # Секции страниц
│   ├── header.tsx         # Навигация
│   ├── footer.tsx        # Футер
│   ├── theme-toggle.tsx  # Переключатель темы
│   └── language-switcher.tsx # Переключатель языка
├── hooks/
│   └── use-theme.ts      # Хук для темы
├── i18n/
│   ├── routing.ts        # Конфигурация маршрутизации
│   └── request.ts        # Конфигурация запросов
├── lib/
│   ├── api.ts            # API клиент для Django
│   └── theme.ts          # Утилиты темы
├── messages/             # Переводы
│   ├── ru.json
│   ├── en.json
│   └── uz.json
└── middleware.ts         # Next.js middleware для i18n
```

## 🔌 Интеграция с Django

API клиент настроен в `lib/api.ts`. Для подключения к Django backend:

1. Создайте `.env.local`:
```env
NEXT_PUBLIC_API_URL=http://localhost:8000
```

2. Используйте `apiClient` в компонентах:
```typescript
import { apiClient } from '@/lib/api';

const response = await apiClient.submitContactForm(formData);
```

## 🎭 Анимации

Используется Framer Motion для плавных анимаций:
- Появление элементов при скролле
- Hover эффекты
- Переходы между страницами
- Плавные изменения темы

## 📱 Адаптивность

Проект полностью адаптивен и работает на:
- 📱 Мобильных устройствах (320px+)
- 📱 Планшетах (768px+)
- 💻 Десктопах (1024px+)
- 🖥️ Больших экранах (1440px+)

## 🔧 Настройка

### Добавление нового языка

1. Добавьте код языка в `i18n/routing.ts`
2. Создайте файл переводов в `messages/[lang].json`
3. Добавьте язык в `components/language-switcher.tsx`

### Добавление новой страницы

1. Создайте файл в `app/[locale]/[page-name]/page.tsx`
2. Добавьте переводы в файлы `messages/*.json`
3. Добавьте ссылку в навигацию (`components/header.tsx`)

## 🚀 Деплой

### Vercel (рекомендуется)

```bash
npm install -g vercel
vercel
```

### Другие платформы

```bash
npm run build
```

Собранные файлы в `.next/` готовы для деплоя на любую платформу, поддерживающую Next.js.

## 📝 TODO

- [ ] Добавить страницы: About, Catalog, Portfolio, Pricing, Contacts
- [ ] Интегрировать с Django API
- [ ] Добавить SEO мета-теги
- [ ] Добавить Google Analytics
- [ ] Оптимизировать изображения
- [ ] Добавить тесты

## 📄 Лицензия

Copyright © 2026 Paradise Accessories
